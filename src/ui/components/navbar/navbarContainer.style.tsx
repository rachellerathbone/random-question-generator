import styled from 'styled-components';

export const NavbarContainer = styled.nav`
  display: flex;
  padding: 1em 0;
`;

export const Brand = styled.div`
  display: flex;
`;

export const NoAwksBrandName = styled.h6`
  align-self: center;
  display: flex;
  font-size: 1.2rem;
  font-weight: 600;
  margin-left: 0.4em;
  text-transform: uppercase;
`;
